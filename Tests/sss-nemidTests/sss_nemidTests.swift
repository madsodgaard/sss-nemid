import XCTest
@testable import sss_nemid

final class sss_nemidTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(sss_nemid().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
